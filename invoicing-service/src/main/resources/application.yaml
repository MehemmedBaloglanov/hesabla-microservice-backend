server:
  port: 8082

spring:
  application:
    name: invoicing-service
  config:
    import: "configserver:http://localhost:8888"
  datasource:
    url: jdbc:postgresql://localhost:5433/invoicing_db
    username: invoicing
    password: invoicing
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
